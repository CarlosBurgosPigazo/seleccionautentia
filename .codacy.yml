engines:
 pmdjava:
   enabled: true
   exclude_paths:
     - config/engines.yml
 duplication:
   enabled: true
   exclude_paths:
     - config/engines.yml
 metrics:
   enabled: true
   exclude_paths:
     - config/engines.yml
 coverage:
   enabled: true
   exclude_paths:
    - config/engines.yml
    - **/config/**
    - **/*.sql
    - **/*.yml
    - **/*.xml
    - **/*.gitignore
    - **/*.xhtml
    - **/*.properties
